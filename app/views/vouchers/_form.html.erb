<%= form_for(@voucher) do |f| %>
  <% if @voucher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@voucher.errors.count, "error") %> prohibited this voucher from being saved:</h2>

      <ul>
      <% @voucher.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :voucher_number, :class => 'input-block-level', :placeholder => 'Voucher Number' %>
  <%= f.text_field :site_source, :class => 'input-block-level', :placeholder => 'Site Source' %>

  <div class="form-actions" style="margin-bottom: 0; padding-bottom: 0;">
    <%= link_to 'Back', vouchers_path, :class => 'btn' %>
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
